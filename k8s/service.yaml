apiVersion: v1
kind: Service
metadata:
  name: gitops-api
  labels:
    app: gitops-api
spec:
  type: NodePort
  selector:
    app: gitops-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080